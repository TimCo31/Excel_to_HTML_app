Application qui permet de transformer une base de données Excel, pour du Discrete Choice Model, "en carte" HTML.

Possibilité de cloner le repo et d'installer les packages via poetry :
    - poetry install
